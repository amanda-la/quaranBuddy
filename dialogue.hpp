#include <string.h>
#include <iostream>
#include <vector>

//dialogue will be held in a vector of strings, different phrases will be "randomly" spat out by the "bot" through the vector

//changed name

std::vector<std::string> changedName;

void initVec(std::vector<std::string> vec);

void addPhrase(std::vector<std::string> vec, std::string phrase);
